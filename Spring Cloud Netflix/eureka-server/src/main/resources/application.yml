server:
  port: 8761
eureka:
  client:
    register-with-eureka: false  #不注册自己
    fetch-registry: false #表示是否要同步其他的注册中心的数据
    service-url:
      defaultZone: http://localhost:8761/eureka/   #注册中心的访问地址